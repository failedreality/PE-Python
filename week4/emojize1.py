# week 4 homework
# this one requires pip install emoji
# also created a .venv for this as well (full subdirectory)

import emoji

display = input('Please input a string with an emoji code: ')
# print(display)

print(emoji.emojize(display))

